# The main job for hydrate.
# This job runs hydrate_pipeline on a schedule.
resources:
  jobs:
    download_job:
      name: download_3w_dataset
      tasks:
        - task_key: download_3w_dataset
          notebook_task:
            notebook_path: /Users/scott.mckean/Repos/hydrate_prediction/download_3w_job.py
